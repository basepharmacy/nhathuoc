import { DataMigrationLayout } from './Layout'

export function DataMigration() {
  // Data fetching sẽ được thêm ở đây khi cần
  // Ví dụ: const { data } = useQuery(...)

  return <DataMigrationLayout />
}
